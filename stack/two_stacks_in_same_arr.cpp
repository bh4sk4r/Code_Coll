#include <bits/stdc++.h>

#define fastio                        \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)

#define endl "\n"

using namespace std;
void push_back(vector<int> &v,int n,int top)
{
    if(top==n-1)
    return;
    v[top] = 
}
int main()
{
    fastio;
    vector<int> 
    return 0;
}